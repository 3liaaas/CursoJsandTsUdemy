const nome1 = 'Elias';// string
const nome2 = 'jão';// string
const nome3 = 'cavalo';// string
const num1 = 10; //number
const num2 = 10.52; //number
let nomeAluno; //undefined -> não aponta local na memória, memória vazia
const sobrenomeAluno = null; // -> não aponta local na memória, memória vazia
const aprovado = false; //booleasn = true ou false (lógico)
console.log(typeof nome1, nome1)